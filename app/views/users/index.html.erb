<h1>List of Users</h1>

<% @users.each do |user| %>
  <label>Name: <%= user.name %></label>
  <label>Email: <%= user.email %></label>
  <%= link_to 'Delete', user_path(user), method: :delete %>
<% end %>