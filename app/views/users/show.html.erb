<h1>Users</h1>

<% @users.each do |user| %>
  <p>
    <%= user.name %>
  </p>
  <p>
    <%= user.email %> 
  </p>
  <p>    
    <%= link_to 'Delete', user_path(user), method: :delete %>
  </p>
<% end %>